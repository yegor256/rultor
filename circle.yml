machine:
  java:
    version: openjdk7
dependencies:
  cache_directories:
    - ~/.m2
general:
  branches:
    ignore:
      - gh-pages
test:
  override:
    - unset GEM_PATH; mvn clean install -Pqulice

